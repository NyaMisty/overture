# appveyor.yml
image: macos

clone_folder: /tmp/src/github.com/shawn1m/overture

environment:
  GOPATH: /tmp/

stack: go 1.13

before_build:
  - "go version"
  - "go env"
  - "ls -al"
  - "python3 ./build.py -build"

artifacts:
  - path: 'overture-*.zip'
